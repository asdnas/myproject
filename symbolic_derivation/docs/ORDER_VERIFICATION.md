# 事件顺序验证

表 A1 约束已录入。当前环境未安装 `scipy`，因此 `order_verifier.py` 使用三变量
线性约束多面体的顶点枚举计算表达式全域最小值/最大值，可判断相邻事件恒定顺序、
反向顺序、顺序不固定及可能重合边界。该方法不是单点数值采样。

对 A-H VoltageSegment，已在各自表 A1 全约束域上验证：

- 8 段持续时间均恒为非负；
- 8 段均可能在模式边界取零，即 `zero_duration_possible=True`；
- 8 段持续时间总和恒为 `Ts`。

D/G 通用生成结果另与 legacy 反推结果逐段一致。完整开关事件层仍不在本阶段范围内。
