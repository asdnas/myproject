# Duty-equivalent 待处理项

已确认 `fai=phi_paper`。当前源码 `2/3` TBPHS 缩放已被符号验证为错误来源。

后续事项：

1. 如需修改 DSP 源码，应把所有有效和备用路径中的 `2/3`/`285.333333` 相位缩放统一改为 `1.0`/`TBPRDVAL`；本阶段未修改 `程序/`。
2. 修改 DSP 后需要通过示波器确认正 fai 时原边超前、副边滞后。
3. corrected 理想模型中开通和关断边界电流均与 TPS 相同。若论文仍研究关断损耗差异，需要加入 dead-band、传播延时或器件换流细节。
