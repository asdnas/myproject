# 时间基准审查

统一定义：

`Ts = 1/fs`

`Th = Ts/2 = 1/(2fs)`

旧程序每段时间系数乘以 `Th`，但 8 段持续时间总和为：

`sum(dt[k]) = 2*Th = Ts`

因此旧程序的 8 段覆盖的是一个**完整开关周期**，不是半周期。后续符号程序统一使用
“完整周期 8 段”表述。

面积约束为：

`sum((I[k]+I[k+1])*dt[k]/2) = 0`

它表示完整周期内电感电流积分为零，即完整周期平均电感电流为零、没有直流偏置。
